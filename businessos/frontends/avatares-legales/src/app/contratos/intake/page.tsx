import { EmptyState } from '@/shared/components/ui'

export default function VistaIntakeOperacion() {
  return (
    <EmptyState
      titulo="Intake de operación"
      descripcion="Vista en construcción — se llena en la fase F5 (Avatar de Contratos)."
    />
  )
}
