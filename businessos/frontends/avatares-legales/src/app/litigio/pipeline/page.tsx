import { EmptyState } from '@/shared/components/ui'

export default function VistaPipelineCasos() {
  return (
    <EmptyState
      titulo="Pipeline de casos"
      descripcion="Vista en construcción — se llena en la fase F4 (Avatar de Litigio)."
    />
  )
}
